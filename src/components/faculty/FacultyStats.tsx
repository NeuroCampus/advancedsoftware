
import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";

const FacultyStats = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Faculty Statistics</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Faculty Statistics component placeholder</p>
      </CardContent>
    </Card>
  );
};

export default FacultyStats;
