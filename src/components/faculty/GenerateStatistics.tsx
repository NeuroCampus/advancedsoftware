
import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";

const GenerateStatistics = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Generate Statistics</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Generate Statistics component placeholder</p>
      </CardContent>
    </Card>
  );
};

export default GenerateStatistics;
