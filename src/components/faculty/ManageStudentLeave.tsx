
import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";

const ManageStudentLeave = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Manage Student Leave</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Manage Student Leave component placeholder</p>
      </CardContent>
    </Card>
  );
};

export default ManageStudentLeave;
