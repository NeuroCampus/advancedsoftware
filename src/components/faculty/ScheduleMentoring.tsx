
import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";

const ScheduleMentoring = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Schedule Mentoring</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Schedule Mentoring component placeholder</p>
      </CardContent>
    </Card>
  );
};

export default ScheduleMentoring;
