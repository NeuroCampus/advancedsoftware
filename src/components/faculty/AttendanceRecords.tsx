
import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";

const AttendanceRecords = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Attendance Records</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Attendance Records component placeholder</p>
      </CardContent>
    </Card>
  );
};

export default AttendanceRecords;
