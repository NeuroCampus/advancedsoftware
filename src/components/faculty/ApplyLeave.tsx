
import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";

const ApplyLeave = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Apply Leave</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Apply Leave component placeholder</p>
      </CardContent>
    </Card>
  );
};

export default ApplyLeave;
