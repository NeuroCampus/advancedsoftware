
import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";

const UploadMarks = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Upload Marks</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Upload Marks component placeholder</p>
      </CardContent>
    </Card>
  );
};

export default UploadMarks;
