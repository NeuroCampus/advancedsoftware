
import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";

const ProctorStudents = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Proctor Students</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Proctor Students component placeholder</p>
      </CardContent>
    </Card>
  );
};

export default ProctorStudents;
