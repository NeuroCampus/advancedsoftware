
import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";

const BranchesManagement = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Branches Management</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Branches Management component placeholder</p>
      </CardContent>
    </Card>
  );
};

export default BranchesManagement;
