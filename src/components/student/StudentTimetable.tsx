
import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";

const StudentTimetable = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Student Timetable</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Student Timetable component placeholder</p>
      </CardContent>
    </Card>
  );
};

export default StudentTimetable;
