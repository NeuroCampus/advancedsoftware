
import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";

const SectionManagement = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Section Management</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Section Management component placeholder</p>
      </CardContent>
    </Card>
  );
};

export default SectionManagement;
