
import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";

const AttendanceView = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Attendance View</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Attendance View component placeholder</p>
      </CardContent>
    </Card>
  );
};

export default AttendanceView;
