
import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";

const MarksView = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Marks View</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Marks View component placeholder</p>
      </CardContent>
    </Card>
  );
};

export default MarksView;
