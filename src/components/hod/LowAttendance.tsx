
import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";

const LowAttendance = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Low Attendance</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Low Attendance component placeholder</p>
      </CardContent>
    </Card>
  );
};

export default LowAttendance;
