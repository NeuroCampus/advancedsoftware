
import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";

const StudentManagement = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Student Management</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Student Management component placeholder</p>
      </CardContent>
    </Card>
  );
};

export default StudentManagement;
