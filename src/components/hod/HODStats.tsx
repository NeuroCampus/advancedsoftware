
import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";

const HODStats = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>HOD Statistics</CardTitle>
      </CardHeader>
      <CardContent>
        <p>HOD Statistics component placeholder</p>
      </CardContent>
    </Card>
  );
};

export default HODStats;
