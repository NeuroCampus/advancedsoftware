
import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";

const LeaveManagement = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Leave Management</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Leave Management component placeholder</p>
      </CardContent>
    </Card>
  );
};

export default LeaveManagement;
